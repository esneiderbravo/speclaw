# Reports — add-multidialect-compiler

Expected discipline reports for archive:

| File | Discipline |
| --- | --- |
| `backend.md` | Parse, compile orchestrator, dialects, import, fragment/idempotence |
| `api.md` | CLI `laws compile` / `laws import` (+ any setup action); no new MCP tools |
| `infra.md` | Optional — doctor always-on budget + managed `ai-specs/rules` layout |

Archive is blocked until at least one report exists.
