# Reports — add-law-integrity

Expected discipline reports for archive:

| File | Discipline |
| --- | --- |
| `backend.md` | lock / scan / integrity / accept wiring |
| `security.md` | threat model, detectors, accept human-only, no MCP mutate |
| `api.md` | CLI verify/laws surface (no new MCP tool) |

Archive is blocked until at least one report exists.
