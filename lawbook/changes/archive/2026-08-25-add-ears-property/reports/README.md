# Reports — add-ears-property

Expected discipline reports for archive:

| File | Discipline |
| --- | --- |
| `backend.md` | `ears.ts`, validate wiring, coverage `ptest` / runner detection, config loaders |
| `api.md` | `lawbook_change` validate/coverage contract (no new MCP tool), CLI messaging |

Archive is blocked until at least one report exists.
