# Reports — add-requirement-coverage

Discipline reports this change will produce during `build`:

- `backend.md` — Compass extract/schema + lawbook coverage engine + archive gate
- `api.md` — MCP `lawbook_coverage` + CLI `speclaw coverage` wire contract
