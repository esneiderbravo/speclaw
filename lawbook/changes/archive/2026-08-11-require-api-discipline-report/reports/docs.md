# Docs checks — require-api-discipline-report (2026-08-11)

Date: 2026-08-11 · Branch: `feat/api-discipline-report` · cwd:
`/Users/esneiderbravo/Projects/speclaw`

Scope: library documentation/rule/skill/config **assets** and their TypeScript
build. This change adds a new always-applied rule and names `api` as a
first-class discipline report across the library. It touches no runtime backend,
frontend, or API surface — so the runtime discipline reports (`backend.md`,
`frontend.md`, `api.md`) do not apply; this `docs.md` records the compile/copy
gates and the asset-propagation verification that stood in.

## Gates & results

| Check | Command | Result |
|-------|---------|--------|
| Format | `npm run check` (Prettier `--check`) | ✅ PASS — "All matched files use Prettier code style!" |
| Lint | `npm run check` (ESLint) | ✅ PASS — no errors |
| Type-check + asset copy | `npm run build` (`tsc && copy-assets`) | ✅ PASS — "copied assets for 3 module(s)" |
| Unit/integration tests | `npm test` (`node --test`, coverage ≥80%) | ✅ PASS — tests 129, pass 129, fail 0; all files 97.90% lines / 92.10% branches / 98.18% funcs |

## Tests added / updated

None. No unit-test runner covers the shipped markdown/YAML assets, and no
existing test asserts the exact text of `CONFIG_YAML`, the mandatory-step
strings, or the rule-file set (verified by grep over `test/`). The suite's
report fixtures use `reports/backend.md`, which this change does not alter, so
the 129 existing tests continue to pass unchanged — confirming the engine's
archive-gate and sync behavior is untouched by the asset/wording edits.

## Spec-scenario coverage

The delta promoted the full `lawbook-workflow` capability. New/changed scenarios
are the discipline-report ones; the rest are carried forward verbatim and remain
covered by the existing engine/tool suite.

| Scenario | Verified by |
|----------|-------------|
| A full-stack change ships backend, frontend, and api reports (new) | `spec-reports-disciplines` rule + `build` SKILL Step 5 + testing-standards frame reports as an open per-discipline set and name `api.md`; propagation confirmed in a throwaway `init` (rule + config + rendered standard carry the api obligation) |
| A discipline beyond the common set gets its own named report (new) | Rule §1 states the set is open ("There is no fixed list") and lists `database.md`/`infra.md`/`security.md`/… as examples; build SKILL, testing-standards, lawbook standard, config generator all reworded to the open set; grep-confirmed in source and `dist/` |
| An endpoint change requires an api report (new) | Rule §2 + build SKILL + testing-standards state `api.md` is mandatory on any API-surface change; text present in `dist/` and in a scaffolded project |
| A backend report does not substitute for the api report (new) | Rule §2 and build SKILL/testing-standards state this explicitly; grep-confirmed in source, `dist/`, and scaffold |
| A change with no API surface may omit the api report (new) | Rule §2 final clause + spec scenario; docs-only nature of this very change exercises the omission (no `api.md` produced, and that is not a defect) |
| Build records evidence of testing; Draft scaffolds the reports folder; A discipline report carries the required sections; Pre-existing failures declared honestly; Every delta-spec scenario is accounted for | This change itself: `draft` scaffolded `reports/`, `build` wrote this structured report, and this table accounts for every scenario |
| A new/existing capability is reported as created/updated | `lawbook_sync` output reported `lawbook-workflow` under `updated` (not `created`) |
| Behavior built after drafting captured before promotion; Unchecked task / Missing reports / README-only / Unsynced specs block archive; A complete change archives; Drift reconciled before archive | Carried forward unchanged; enforced deterministically in `engine.js` and covered by `test/unit/engine.test.ts` (archive-precondition and sync tests, all green in the 129-test run) |
| Verification isolated by default; real-data write gated on authorization; no raw store commands unprompted; report records how verification stayed safe | Carried forward unchanged; honored here — verification ran in a `mktemp` throwaway git repo, removed after, touching no real store (recorded below) |
| Draft/Explore refresh the index first; Draft reuses capability by exact name; Draft grounds delta in canonical; Validate warns on near-duplicate / dropped requirements / raises none when exact | Carried forward unchanged; honored in this change's own draft (`compass_index` run first; delta placed under the exact `lawbook-workflow` folder, started from canonical; `lawbook_validate` returned valid with 0 warnings) |

## Pre-existing / unrelated failures

None. All gates green on the first run.

## Pending manual steps

None. Note: the repo's own `ai-specs/` mirror is gitignored (regenerated by
init/update) and root-owned on this host; it was not hand-edited. Propagation was
instead proven by a throwaway `dist/cli/index.js init`, which produced
`ai-specs/rules/spec-reports-disciplines.md` and the api-aware `config.yaml` and
`docs/standards/testing-standards.md`. Verification was isolated by construction:
a `mktemp -d` git repo, deleted after the check — no real data store touched.

## Verdict

PASS — the API discipline report is codified as a shipped rule and named across
the build/draft skills, testing standards, lawbook standard, config generator,
and the canonical spec; all gates green and propagation verified in an isolated
scaffold.
