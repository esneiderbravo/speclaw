# Tasks — require an API discipline report

- [x] **Step 0 — Create the feature branch (first).** `git checkout -b
      feat/api-discipline-report` off `main`.

- [x] Add the new rule `spec-reports-disciplines.md` to the source of truth
      `src/modules/lawbook/assets/rules/`. It states: reports are per-discipline;
      `backend` / `frontend` / `api` are the recognized set; each is mandatory
      when the change touches it; `api.md` is mandatory whenever the change
      touches an API surface and no other report substitutes for it; and what
      `api.md` must document. (The self-hosted `ai-specs/` mirror is gitignored —
      regenerated by init/update — so it is not hand-edited; propagation verified
      via a throwaway `init`.)

- [x] Update the `build` skill Step 5 in
      `src/modules/lawbook/assets/skills/build/SKILL.md` — name `api.md` in the
      discipline list and state the API-surface obligation.

- [x] Update the `draft` skill reports-scaffold guidance in
      `src/modules/lawbook/assets/skills/draft/SKILL.md` to name `api.md` and its
      API-surface requirement.

- [x] Update the reports section of `docs/standards/testing-standards.md` and its
      shipped template
      `src/modules/foundation/assets/docs/standards/testing-standards.template.md`
      — name `api.md` and add the API-surface obligation. Also updated the
      `docs/standards/lawbook.md` reports paragraph and its template.

- [x] Update the report bullet in the existing `spec-tasks-mandatory-steps.md`
      rule (asset) so it names `backend` / `frontend` / `api` consistently with
      the new rule.

- [x] Reword the mandatory reports step in `lawbook/config.yaml` and its
      generator `src/modules/lawbook/engine.ts` (`CONFIG_YAML`) to name
      backend / frontend / api explicitly.

- [x] Review and update the affected tests. No unit-test runner covers the
      markdown/YAML assets and no test asserts the config text or rule-file set
      (grep-confirmed); the 129 existing tests pass unchanged.

- [x] Run the quality gates and verify they pass — `npm run check` (Prettier +
      ESLint) ✅, `npm run build` (strict `tsc` + asset copy) ✅, `npm test`
      (129/129) ✅. Real output captured in `reports/docs.md`.

- [x] Perform manual verification — ran `npm run build`, confirmed the new rule
      and updated assets are in `dist/`, and scaffolded a throwaway `mktemp` git
      repo where the rule landed in `ai-specs/rules/` and `config.yaml` /
      `docs/standards/testing-standards.md` carry the api obligation. No real
      store touched; throwaway dir removed.

- [x] Produce the discipline report under `reports/docs.md` following the
      required structure (this change touches docs/rules/config assets and the TS
      build, not a runtime backend/frontend/api surface).

- [x] Run `lawbook_sync` to promote the delta spec into
      `lawbook/specs/lawbook-workflow/spec.md` (reported as updated).

- [x] Update the technical documentation the change touches (README /
      `docs/standards/` references to the report disciplines are now consistent —
      testing-standards and lawbook standards updated).

- [x] Archive the change within the same PR (`lawbook:archive` /
      `lawbook_archive`) — only after tasks are checked, a report exists, and the
      specs are synced.
