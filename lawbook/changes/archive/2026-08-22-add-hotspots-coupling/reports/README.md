# Reports — add-hotspots-coupling

Discipline reports this change will produce during `build`:

- `backend.md` — extract metrics, hotspots/coupling engines, git-history extensions
- `api.md` — MCP `compass_hotspots` / `compass_coupling` + CLI verbs
- `database.md` — schema `"8"`, `node_metrics`, forced reindex

Required report structure (filled by `build`): header · gates table · tests
added · spec-scenario coverage · pre-existing failures · pending manual ·
verdict.
